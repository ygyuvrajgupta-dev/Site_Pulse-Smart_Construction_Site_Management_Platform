import { Router } from "express";

/**
 * Lead routes.
 * Endpoints:
 *   GET    /api/v1/leads
 *   POST   /api/v1/leads
 *   GET    /api/v1/leads/:id
 *   PUT    /api/v1/leads/:id
 *   DELETE /api/v1/leads/:id
 */
const router = Router();

// Routes will be added here

export default router;
