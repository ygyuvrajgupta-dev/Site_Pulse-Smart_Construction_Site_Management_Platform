import { Router } from "express";

/**
 * User routes.
 * Endpoints:
 *   GET    /api/v1/users
 *   GET    /api/v1/users/:id
 *   PUT    /api/v1/users/:id
 *   DELETE /api/v1/users/:id
 */
const router = Router();

// Routes will be added here

export default router;